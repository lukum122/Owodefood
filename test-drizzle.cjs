const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres:Luktayo%40123@127.0.0.1:5432/postgres' }); const { drizzle } = require('drizzle-orm/node-postgres'); const { pgTable, text, jsonb } = require('drizzle-orm/pg-core'); const products = pgTable('products', { id: text('id').primaryKey(), addonGroups: jsonb('addon_groups') }); const db = drizzle(pool); db.select().from(products).limit(1).then(res => { console.log('Drizzle result:', res); pool.end(); }).catch(console.error);
